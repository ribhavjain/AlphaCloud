import { LogFilterPipe } from './log-filter.pipe';

describe('LogFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LogFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
